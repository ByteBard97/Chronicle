## 2026-08-20 afed3834

Bootstrapped the Chronicle project (external Python social-simulation service for Skyrim SE/AE) from a fully-specified kickoff prompt the user had already drafted in a separate claude.ai web conversation. Confirmed scope with the user (full scaffold + ingest existing research now) before acting, since the actual repo state didn't match the kickoff prompt's assumption of an empty repo — `research/` already had 6 raw report files dropped in, and a 7th (plus a .zip, likely containing `fig1_tier_architecture.png`) landed mid-session, confirming research generation is still running in parallel elsewhere.

Built the full scaffold: git init, uv-managed Python 3.12 project (pytest + ruff dev deps), `chronicle/` package with `events.py` (append-only `EventLog`, `Event`/`NPCDied`/`CrimeWitnessed`/`RumorHeard` dataclasses) and one passing pytest locking in the event-sourcing pattern, `docs/vision.md` and `docs/architecture.md`, ADRs 0001 (external service) and 0002 (event sourcing) as accepted, ADR 0003 (substrate choice: Skyrim vs. prototype elsewhere) left as DRAFT per the user's explicit instruction — do not resolve it without approval. Also `dashboard/`, `adapters/skyrim/`, `scenarios/`, `notes/{inbox,daily}` with stub READMEs, a Makefile (test/lint/sim targets), and `docs/decisions/open-questions.md` as an empty placeholder for research-surfaced contradictions.

Mapped the 7 raw research files to the 4 original prompt topics: Skyrim modding substrate got 2 independent reports (gemini version + a compass_artifact version), social-sim literature got 1, hybrid LLM+symbolic got 3 (high redundancy — RumorSphere/HiSim/AgentTorch as shared anchors across all three), voice pipeline got 1. Dispatched 4 parallel fresh general-purpose agents (not forks, since each only needs its own slice of raw research, not this conversation's context) to read their assigned raw file(s) and file merged, findings-tagged summaries into `docs/research/01..04-*.md` with frontmatter, explicitly instructed to preserve concrete citations/links/version numbers rather than paraphrase, and to flag disagreements between reports on the same topic rather than silently picking one.

**Pending when this resumes:** wait for the 4 ingestion agents to report back, then (1) build `docs/research/00-index.md` merging all tagged findings across reports, (2) check whether any report's findings land in `docs/decisions/open-questions.md` (the two Skyrim-substrate reports may already disagree on how strongly to recommend building on SkyrimNet's extension API vs. treating it as study-only — need to check the agents' contradiction flags), (3) evaluate whether findings resolve ADR-0003 (report 1's SkyrimNet finding is likely relevant) and propose — not finalize — an ADR update per the user's standing workflow instructions. Have not yet made a git commit; scaffold is fully untracked as of this write.

---

All 4 ingestion agents returned; `docs/research/01-04-*.md` filed plus `00-index.md` merging tagged findings across reports. Report 01 (Skyrim substrate) surfaced a real architectural disagreement between its two source reports on SkyrimNet (build-on vs. study-only) — logged, not resolved, in `docs/decisions/open-questions.md`, since it bears on ADR-0003 which stays DRAFT.

The user then reviewed all six raw reports on their end (in the parallel web conversation) and sent detailed feedback mid-turn: three of the report disagreements were factual, not genuinely ambiguous (MinAI deprecation status, Mantella's exact latest-release date, CHIM's license), so instead of re-running research I verified them directly against the GitHub API (`gh api repos/...`) — confirmed MinAI deprecated (README banner, no LICENSE, last release 2.1.3 / 2025-04-23), Mantella v0.14 published 2026-04-21, CHIM/HerikaServer MIT-licensed (`MIT-LICENSE` file present). Corrected `01-skyrim-modding-substrate.md` in place and logged the verification in `open-questions.md` under "Resolved by direct repo verification."

The user also identified a real gap none of the four original research prompts covered: **save/reload timeline consistency** — Chronicle's event-sourced log will contain events that never happened in the player's timeline after a reload, and nobody researched how Mantella/CHIM/SkyrimNet handle this today, or whether SKSE co-save serialization is the right anchor for timeline identification. Assessed as possibly the hardest unsolved integration problem in the project. Drafted a full research prompt for it in `open-questions.md`, ready to fire — not yet run.

Also drafted (not yet run) a smaller SkyrimNet ecosystem due-diligence prompt (sustainability, API-breakage history, single-maintainer risk) since SkyrimNet is simultaneously the best integration target and the biggest platform risk (closed-source DLL, no LICENSE, one maintainer) — this is meant to risk-size before ADR-0003 gets finalized, not to re-architect.

Noted economic simulation (prices/supply/trade-ripple) as a deliberately deferred v0.4 gap in `open-questions.md` so it doesn't silently disappear from the plan.

**Still pending:** the two batch-2 research prompts (SkyrimNet health, save/reload consistency) haven't been fired yet — that happens in the other (web) conversation, and their results come back through the same ingestion workflow. ADR-0003 stays DRAFT until at least the SkyrimNet due-diligence prompt returns. No git commit made yet this session — worth doing once the user confirms the scaffold + batch-1 ingestion looks right.

---

Committed the scaffold + batch-1 research (43 files) as the repo's root commit. The user then reported that the save/reload research had *already* come back (two independent reports, dropped into `research/`) and that a "gap the four original prompts missed" note from my prior update was stale — I'd flagged it as still-queued when it was actually already answered. Corrected course: read both reports in full (`notes/inbox/save-reload-compass.md`, `.../save-reload-architecture.md` — moved there per the user's instruction so a fresh Claude Code pass could read them directly rather than have 60KB pasted in-chat), filed them as `docs/research/05-save-reload-sync-protocol.md` and `06-save-reload-timeline-sync.md`.

Both reports independently converged on the same core design (event-sourced service, save-embedded `SaveUUID`+`generation` identity via SKSE co-save, fork-on-reload never rollback, tombstone-then-reap branch GC) but diverged on one point worth tracking rather than silently resolving: they characterize SkyrimNet's own reload behavior differently (report 05: effectively no cross-process fork mechanism; report 06: an explicit but weakly-sourced "timeline cleanup protocol"). Logged that as a sub-disagreement in `open-questions.md`, closed the save/reload item there, and carried forward three implementation-risk uncertainties the reports themselves flagged as unconfirmed (CHIM's fork-trigger threshold is reconstructed not measured; the save-UUID-in-cosave pattern has no confirmed Skyrim precedent, nearest prior art is an NWN module; the `.skse`/`.ess` pairing is atomic by convention, not guaranteed).

Wrote **ADR-0004 (Timeline branching)**: Skyrim's save topology is a DAG; every event carries a `(save_uuid, generation)` branch key; reload forks a new generation rather than rewriting history; state derivation is a root→head lineage traversal, not a fold over the whole log; abandoned branches get tombstoned then reaped once no live save references them. And **ADR-0005 (Sync handshake)**: writes gate on an explicit watermark/`TIMELINE_READY` handoff plus epoch fencing, never on `kPostLoadGame` alone (which can fire before Papyrus is ready and never fires on new-game); `OnInit()` vs `OnPlayerLoadGame()` covers new-game vs. reload; a `g_isLoading` flag suppresses event generation during the load window. Both ADRs marked `accepted` — these aren't speculative, both source reports converged independently.

Amended `chronicle/events.py` to match: `Event` now carries `save_uuid`/`generation`; `EventLog` is branch-aware with `append`/`fork`/`lineage` (root→head traversal) instead of a flat list, plus `all()` kept for cross-branch debugging/GC use. Rewrote `chronicle/tests/test_events.py`'s existing test for the new constructor signature and added `test_forking_a_branch_excludes_the_abandoned_suffix`, which forks mid-log and asserts the mutated third event is excluded from the new branch's lineage but still recoverable via `log.all()` and the original branch. Both tests pass; `ruff check` clean.

Added the FormID rule to `docs/architecture.md` (report 06's contribution, absent from every other filed report): never persist raw FormIDs, since they're load-order-relative — store plugin-name + local-offset composite keys instead, resolved only at the `adapters/skyrim/` boundary. Flagged this as load-bearing for ADR-0004, since a raw-FormID leak after a load-order change would look exactly like a timeline-fork bug.

Archived the two raw save/reload reports from `notes/inbox/` into `research/` (matching where the other raw reports already live) once filed, keeping `notes/inbox/` empty again per its stated purpose.

**Still pending:** the SkyrimNet ecosystem due-diligence prompt (drafted in `open-questions.md`, ready to fire in the other conversation) is now the only queued research task, and it gates ADR-0003 (substrate choice), which stays DRAFT. The user also mentioned two more research reports are outstanding from "kimi" — not yet seen; check `research/` and `notes/inbox/` for new files next session if they haven't arrived by then. Uncommitted at time of this write: everything from this second half of the session (ADR-0004/0005, reports 05/06, the events.py change, the open-questions/index/architecture edits) — worth committing once reviewed.

---

Research phase closed out. The final two reports (SkyrimNet due-diligence, literature v2) landed directly in `research/` rather than `notes/inbox/` as the user's instruction assumed — found them by listing the directory rather than waiting. Read both in full, filed as `docs/research/07-skyrimnet-substrate.md` and `08-social-sim-literature-v2.md`.

Report 07 resolved ADR-0003 decisively: direct SkyrimNet coupling is HIGH RISK (closed C++ core, no LICENSE file, single Ko-fi-funded maintainer, documented API-version drift across betas, in-process crash-to-desktop failure mode), and the recommended Substrate Abstraction Layer (SkyrimNet as primary provider, powerofthree's Extender + open SKSE_HTTP as secondary, both behind a generic Python provider interface, both under `adapters/skyrim/`) is MEDIUM RISK — adopted. Rewrote ADR-0003 from DRAFT to `accepted` with the full risk rationale. Report 07 also independently re-derived the save/reload sync design (its own `ChronicleSync::OnGameSave`/`OnGameLoad` C++ sketch, its own `SAVE_NOTIFY`/`LOAD_REQUEST`/`SYNC_READY` protocol) while researching an unrelated question — a third convergence, noted in both ADR-0004 and ADR-0005's rationale sections.

Report 08 was the strongest single document the whole research phase produced: a five-layer data-ownership model (canonical events → claims/variants → subjective beliefs → social state → narrative/query), the sparse-graph rule (never a complete N×N matrix over ~1,000 NPCs, evidenced by Socialog's 15-25ms→600ms per-tick cost growth from 50→450 characters), observer-local Beta-distribution reputation (never one global score), a deterministic seeded mutation policy keyed to fuzzy-trace theory's verbatim/gist split, a compact rumor state machine, and CK3-style typed obligation/grudge/hook records. Wrote **ADR-0006 (Data ownership layers)** from this — the sparse-graph and observer-local-reputation rules are called out as load-bearing, not optimizations to add later. Wrote **ADR-0007 (Inspectability)** from the report's "every social outcome must answer who/what-evidence/through-whom/since-when" requirement, tying it directly to the dashboard's causality-timeline as a concrete, checkable definition rather than an aspirational one. Checked the citation for Gossamer (Kreminski, IEEE CoG 2023) since the user flagged it as a new load-bearing name — legitimate, peer-reviewed, PDF link resolves.

Updated `docs/architecture.md` with a Substrate Abstraction Layer section, a Data ownership/inspectability section, and report 08's build-first/add-next/defer list (canonical events + Gossamer's gossip loop + sparse relationships + bounded memory/mutation + typed obligations + observer-local reputation first; rumor-state math and face-threat scoring next; LLM integration explicitly last, confirmed independently by both the hybrid-architecture reports and this literature report). Amended `chronicle/events.py`'s module docstring to place it explicitly as layer 1 of the five-layer model, cross-referencing where layers 2-5 will live and why canonical events staying immutable is what makes ADR-0007's provenance drill-down answerable.

Closed the SkyrimNet item in `open-questions.md` and added a status line at the top: research phase complete, only the deferred economy tier remains open (deliberately, not as an unresolved question). Tests pass, ruff clean.

**Next**: commit this batch, then the project exits research and enters build. First code milestone per the now-settled build order: the claim/variant/belief-instance schema (ADR-0006 layers 2-3) plus a regression scenario suite, no LLM anywhere near it yet.

---

A fourth save/reload report landed (`research/External World-State Sync for Skyrim - Research Report.md`, found by checking `research/` directly rather than trusting the assumed `notes/inbox/` location again). Unlike 05-07, it's grounded in repository forensics — specific GitHub issue/PR numbers (SkyrimNet #251, #487, #119, #391; CHIM/HerikaServer PR #560, #572, #558, #681) rather than docs/FAQs — which let it resolve something 05 and 06 left open: SkyrimNet's actual reload behavior. It *asks* the player before clearing (`ClearTimelineMessage`/`msgClearHistory`, confirmed via issue #251) — neither pure silence (05's read) nor a fully automatic purge (06's read). Filed as `docs/research/09-save-sync-forensics.md`; closed that sub-disagreement in `open-questions.md` with the citation.

The most consequential new find is SkyrimNet issue #487: a 100%-reproducible bug where a GC routine hard-deleted 237 externally-created memories because its liveness criterion was an internal, non-inspectable timestamp that a second write path stamped differently — no recovery path. This is a concrete precedent for exactly the failure ADR-0004's GC design already aimed to prevent, so I sharpened that ADR rather than just filing the report: added a **mandatory, never-NULL bitemporal columns rule** (`gamets`=valid time, `wall_ts`=transaction time — citing CHIM PR #572's NULL-timestamp bug as the reason this can't be optional), made the **reachability-only GC rule** explicit with git's own reflog/grace-period model as a second independent precedent, and added a **protected-stream-class rule** for user-curated content (exempt by construction, not heuristic — both SkyrimNet Beta20 and CHIM's Playthrough Manager are retrofits of this same rule, evidence that heuristic exemptions get missed).

Upgraded ADR-0005 substantially: added the concrete ~100-byte co-save manifest schema (`format_version`, `save_uuid`, `generation`, `parent_generation`, `head_seq`-as-last-ACKed, `gamets`, `wall_ts`, `char_name_hash`-display-only), formalized the existing watermark handshake as HELLO/RESOLVE/ACK with report 09's six-way decision table (CONTINUE/FORK/ADOPT/NEW TIMELINE/LEGACY IMPORT/DEGRADED), added the death-retry-silent-vs-large-jump-confirm rule, and named the "never block" principle explicitly (game side optimistic and async, service side pessimistic and validates against the ACKed branch head).

Converted report 09's failure matrix and race catalog into 12 named scenario stubs under `scenarios/sync/` (markdown specs for now — no scenario runner exists yet to make them executable, but each names a specific failure/race, the expected outcome, and its research citation, so the protocol's claims stay testable rather than asserted).

Amended `chronicle/events.py`: `Event` now carries `seq: int` (per-branch monotonic idempotency counter) plus mandatory `gamets`/`wall_ts` bitemporal fields — all required, no defaults, matching the "never NULL" rule. `EventLog.append()` is now idempotent on `(save_uuid, generation, seq)`, returning `False` on a duplicate rather than double-appending. Updated both existing tests for the new required fields and added `test_append_is_idempotent_on_branch_and_seq`. Three tests pass, ruff clean.

Then, before committing, the user relayed a broader status/planning message from the web conversation: research is now assessed as fully done, with a ledger of what's decided (0001-0007, the three-tier belief architecture, build order, voice pipeline) vs. genuinely-undecided *design* questions for a v0.1 spec (world scope: one hold vs. all ~1,000 NPCs; which 3-5 event types define the minimum cascade; the first player-visible payoff; which ~20 rules make the cut; rumor representation; how long to stay headless before touching the SKSE seam). The web conversation's recommended starting position: one hold, three event types, headless-first with map-view-only dashboard, typed claim slots, prompt-injection deferred until the scenario suite is green — offered as a position to react against, not a final answer. Also flagged: whether to download source literature locally (Game AI Pro 3 ch. 37, Ryan et al. 2015 AIIDE, both City of Gangsters AIIDE 2021 papers, the Gossamer CoG 2023 paper, and optionally clone the Neighborly/Minerva/Drolta repos) — recommended as a `docs/research/papers/README.md` task, PDFs only where clearly open-access.

**Next**: commit the current batch (report 09, ADR-0004/0005 upgrades, scenario stubs, events.py idempotency), then take up the papers-fetching task, then the v0.1-spec conversation is the actual next milestone — not yet started.

---

Committed the report-09/ADR-0004/0005-upgrade/scenario-stubs/events.py-idempotency batch (`98c89b2`). Then fetched the papers the web conversation flagged as worth keeping locally. Checked network/scripted-access for each candidate rather than assuming: `ojs.aaai.org` (AIIDE proceedings) and the authors' own sites (robert.zubek.net, mkremins.github.io) allow direct curl access; gameaipro.com (Game AI Pro 3 ch. 37) and escholarship.org (Ryan's dissertation) both block scripted requests (403 / WAF challenge respectively) — left those two as link-only in `docs/research/papers/README.md` rather than fighting the block. Downloaded and verified (via `file`) as real PDFs: Ryan et al. 2015 (AIIDE, the evidence-typology paper), both City of Gangsters AIIDE 2021 papers (Zubek et al.'s logic-programming architecture + Robison et al.'s design lessons), and Kreminski's Gossamer paper. Noted Neighborly/Minerva/Drolta as MIT-licensed repos worth cloning when the narrative/query layer is actually being built, not vendored speculatively now. Committed as `be6abd2`.

**State now**: research phase and its immediate follow-ups (save-sync forensics, papers) are fully closed out. The actual next step is the one the web conversation surfaced but didn't resolve: a v0.1 spec, meaning explicit answers to six design (not research) questions — world scope (one hold vs. all ~1,000 NPCs), which 3-5 event types define the minimum cascade, the first player-visible payoff, which ~20 rules make the cut, rumor representation (feature-vector-with-mutation vs. typed claim slots), and how long to stay headless before touching the SKSE seam. Haven't started that conversation with the user yet — it's the natural next turn.

---

One more report landed before the v0.1 conversation started: a second, deeper SkyrimNet health deep-dive (`docs/research/10-skyrimnet-health.md`), and this one actually changes an accepted decision rather than just adding detail. Report 07 had rated direct SkyrimNet coupling HIGH RISK but still recommended it as the SAL's *primary* provider. Report 10 — working from actual release history (public C++ API v6→v9 in about a month, Beta21's explicit breaking-changes section) and concrete integrator bug trails (IntelEngine hard-gated on a specific version and once blocked on an unreleased build; SeverActions hit an init-ordering deadlock and a schema break) — found the churn itself, not just the licensing/bus-factor risk, disqualifies SkyrimNet as a foundation. Inverted the priority: **the standalone powerofthree's-Extender + SKSE_HTTP path is now the reference implementation, SkyrimNet becomes an optional adapter pinned to one beta and API version with a startup handshake that refuses to run on mismatch.** Verified the report's central negative claim is at least honestly scoped — it explicitly says the exhaustive search covered public sources only (GitHub/docs/FAQ/Patreon/Ko-fi/Reddit), not the invite-only Discord, which is exactly why "ask Min directly" is the recommended action rather than treating the negative result as final.

Amended ADR-0003 in place (added an "Amendment" section rather than silently rewriting — the original tension/history stays visible) with the full evidence and report 10's concrete promote/drop thresholds (promote SkyrimNet if a real license + succession commitment + API stabilization to semver-with-no-breaking-bumps-across-2-3-cycles; drop the adapter if cadence stalls 2-3+ months or breaking bumps continue every release). Updated `docs/architecture.md`'s SAL section to match, and added the three adapter-isolation tactics as named rules: startup version handshake, all `Register*` calls isolated in one contract-tested module, explicit init-ordering guard (citing the SeverActions deadlock), never redistribute the DLL. Added the Discord-outreach action item to `notes/ideas.md`, closed (re-closed, with updated content) the SkyrimNet item in `open-questions.md`. Tests still pass, ruff clean.

**This is genuinely the last research input** — the user's own framing, and I have no reason to doubt it. Research phase (10 reports across 5 batches) and every ADR it produced or amended (0001-0007 plus this 0003 amendment) are done. Nothing left is a research question; everything remaining is the six v0.1 design decisions (world scope, event set, first payoff, rule contents, rumor representation, headless duration) — explicitly the user's/mine to make, not something to send back to the web conversation. Not yet started.
